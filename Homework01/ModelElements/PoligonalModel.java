package Homework01.ModelElements;

// Syntax error on token ",", permits expectedJava(1610612940)
// Почему после 'extends Poligon' в запятой якобы ошибка?
public class PoligonalModel extends Poligon {
    Poligon Poligons;
    Texture Textures;
}
